namespace Backend.Common.Enums;

public enum StepType
{
    Harvest,        // recoltă (origine)
    Warehouse,      // staționare în depozit
    Transport,      // transport între locații
    Retail          // magazin (destinație)
}